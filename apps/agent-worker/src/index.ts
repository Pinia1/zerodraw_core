/**
 * 独立 Agent Worker 服务入口（占位）。
 * 当前共享 runtime 通过 `@zeroDraw/agent-worker/runtime` 与 `/worker` 导出；
 * API 默认 `AGENT_RUNTIME_HOST=inprocess` 可不部署本服务。
 */
export const AGENT_WORKER_STANDALONE = false;
